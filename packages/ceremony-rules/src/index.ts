/**
 * Per-ceremony evaluation config (sasih rules, dewasa ayu/ala, scoring weights).
 * Placeholder — real ceremony configs land with the engine work (ENG-001 / PRD §7.6).
 */
import type { SemVer } from '@dewasa-ayu/types';

export const CEREMONY_RULES_VERSION: SemVer = '0.0.0';
